type catchRequestType = {
	error: {
		message: string;
		status: number;
	};
};

export function catchRequest<T>(fn): Promise<T> {
	try {

		if(!res.ok){
      res.status

    }
	} catch (error: unknown) {
    instanceof ZodError


    instanceof Error
		return {
			error: {
				message: (error as Error).message,
				status: (error as Error).status
			},
		};
	}
}
